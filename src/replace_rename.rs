use std::path::{Path, PathBuf};

use regex::Regex;

// struct ReplaceRename {
//     path_strs: Vec<String>,
//     regex: Regex,
//     replace: Vec<u8>
// }

// impl ReplaceRename {
//     fn new(pattern: &str, replace: &str) -> Self {

//     }
// }
